<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EgoTools.Views.BatteryControlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EgoTools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls ="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Vertical" Spacing="8">
            <TextBlock Text="电源管理" FontSize="24" Margin="36,32,0,12"/>
            
            <!--获取权限控件-->
            <controls:SettingsCard 
                Header="取得UAC权限"
                Description="修改电池阈值相关设置，需要获取系统UAC权限"  
                Margin="36,8,24,0" 
                HorizontalAlignment="Stretch">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEA18;"/>
                </controls:SettingsCard.HeaderIcon>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE73E;" Visibility="Collapsed" x:Name="UAC_CheckMark"/>
                    <Button x:Name="UacButton" Content="获取" Click="UacButton_Click"/>
                </StackPanel>
            </controls:SettingsCard>
            
            <!--电池控制控件-->
            <controls:SettingsExpander 
                    x:Name="BatteryControlCard"
                    VerticalAlignment="Top"
                    Padding="16"
                    Margin="36,8,24,0" 
                    Header="智能充电"
                    Description="设置电池充电阈值，延长电池寿命"
                    IsEnabled="True"
                    IsExpanded="True">
                <StackPanel Orientation="Horizontal">
                    <ToggleSwitch x:Name="AutoChargeToggle" OffContent="关" OnContent="开" IsOn="False"/>
                    <ProgressRing x:Name="AutoChargeProgressRing"  IsActive="False" Width="32"/>
                </StackPanel>
                <controls:SettingsExpander.Items>
                    <controls:SettingsCard x:Name="ChargeManualContorlCard" Header="调整充电上限">
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                            <FontIcon x:Name="BatteryIcon" Glyph="&#xEBB0;" FontSize="18"/>
                            <TextBlock x:Name="ChargeLimit" Text="80" FontSize="14" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <ProgressRing x:Name="ChargeLimitProgressRing" IsActive="False" Visibility="Collapsed" Foreground="{ThemeResource SystemAccentColor}" FontSize="16"/>
                            <FontIcon x:Name="ChargeLimitCompleteIcon" Glyph="&#xE73E;" Visibility="Collapsed" Foreground="{ThemeResource SystemAccentColor}" FontSize="16"/>
                            <Slider x:Name="ChargeLimitSlider" IsEnabled="False" Minimum="50" Maximum="100" Value="80" Width="200" ManipulationMode="All" TickPlacement="Inline" TickFrequency="10"/>
                        </StackPanel>
                    </controls:SettingsCard>
                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>
        </StackPanel>
    </Grid>
</Page>