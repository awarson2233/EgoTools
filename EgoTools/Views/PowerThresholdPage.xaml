<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EgoTools.Views.PowerThresholdPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EgoTools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
      <StackPanel Orientation="Vertical">
        <TextBlock Text="电源阈值" FontSize="24" Margin="16,16,0,0"/>
        <Border CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16" Margin="28,8,24,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" HorizontalAlignment="Stretch">
          <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xEA18;" FontSize="20" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="12,0,0,0">
              <TextBlock Text="取得UAC权限" FontSize="14" FontWeight="SemiBold"/>
              <TextBlock Text="修改电池阈值相关设置，需要获取系统UAC权限" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,0,0" x:Name="UacButton">
              <TextBlock Text="获取"/>
            </Button>
          </Grid>
        </Border>
        <Border CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16" Margin="28,8,24,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" HorizontalAlignment="Stretch">
          <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xEBC0;" FontSize="20" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="12,0,0,0">
              <TextBlock Text="电池阈值设置" FontSize="14" FontWeight="SemiBold"/>
              <TextBlock Text="设置电池充电阈值，延长电池寿命" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
              <FontIcon x:Name="BatteryIcon" Glyph="&#xEBB0;" FontSize="18"/>
                        <TextBlock x:Name="ThresholdValueText" Text="80" FontSize="14" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
              <Slider x:Name="ThresholdSlider" Minimum="50" Maximum="100" Value="80" Width="200" ValueChanged="ThresholdSlider_ValueChanged" ManipulationMode="All"/>
            </StackPanel>
          </Grid>
        </Border>
      </StackPanel>
    </Grid>
</Page>
