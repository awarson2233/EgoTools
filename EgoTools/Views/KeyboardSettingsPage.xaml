<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EgoTools.Views.KeyboardSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EgoTools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
      <StackPanel Orientation="Vertical">
        <TextBlock Text="键盘设置" FontSize="24" Margin="16,16,0,0"/>
        <Border CornerRadius="6" Background="White" Padding="16" Margin="20,18,24,0" BorderBrush="LightGray" BorderThickness="0.75" HorizontalAlignment="Stretch">
          <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xE765;" FontSize="20" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="12,0,0,0">
              <TextBlock Text="键盘分离支持" FontSize="14" FontWeight="SemiBold"/>
              <TextBlock Text="启用或禁用分离键盘支持" FontSize="12" Foreground="#666666" Margin="0,4,0,0"/>
            </StackPanel>
                    <ToggleSwitch x:Name="KeyboardDetachmentSwitch" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" OnContent="开启" OffContent="关闭" Margin="0,0,-64,0"/>
          </Grid>
        </Border>
      </StackPanel>
    </Grid>
</Page>
